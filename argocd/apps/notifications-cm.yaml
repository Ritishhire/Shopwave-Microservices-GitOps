apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-notifications-cm
  namespace: argocd
data:
  # ── 1. SMTP EMAIL SERVER CONFIGURATION ───────────────────────
  service.email: |
    host: smtp.gmail.com
    port: 587
    from: $email-username
    username: $email-username
    password: $email-password

  # ── 2. EMAIL TEMPLATES ──────────────────────────────────────
  template.app-sync-succeeded: |
    email:
      subject: "✅ ShopWave Deployment SUCCESS"
    message: |
      Application {{.app.metadata.name}} deployed successfully!
      Revision: {{.app.status.sync.revision}}

  template.app-sync-failed: |
    email:
      subject: "❌ ShopWave Deployment FAILED"
    message: |
      Application {{.app.metadata.name}} failed to sync. 
      Please check ArgoCD UI.

  template.app-health-degraded: |
    email:
      subject: "⚠️ ShopWave Health DEGRADED"
    message: |
      Application {{.app.metadata.name}} health is Degraded. 
      Pods might be crashing.

  # ── 3. TRIGGERS (When to send emails) ───────────────────────
  trigger.on-sync-succeeded: |
    - when: app.status.operationState.phase in ['Succeeded']
      send: [app-sync-succeeded]

  trigger.on-sync-failed: |
    - when: app.status.operationState.phase in ['Error', 'Failed']
      send: [app-sync-failed]

  trigger.on-health-degraded: |
    - when: app.status.health.status == 'Degraded'
      send: [app-health-degraded]
